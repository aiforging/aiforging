<!--
  AI Forging feature index — GENERATED. Do not edit by hand.

  Rebuilt by /aiforging:resume (and by /aiforging:forge when it creates a feature)
  from the feature folders and from git. Every column is derived, so nothing here is
  a source of truth and the whole file can be deleted at any time — it will be
  regenerated on the next run.

  Why generated rather than maintained: a hand-kept status column is wrong within a
  month, and a stale status is worse than none — it makes someone skip a feature that
  actually needed them.
-->

# Features

Every feature in this workspace. Run `/aiforging:resume <name>` to pick one up, or
`/aiforging:resume` with no argument to choose from this list.

**Status** is the count of checked slices across the feature's `plan.md` (or across every
work item's plan, for a nested feature) — it is the plan's own checkboxes, counted, not a
label anyone maintains.

| Feature | Status | Last activity | Summary |
|---|---|---|---|
| _(no features yet — `/aiforging:forge <name> "<what you want>"` creates the first)_ | | | |

<!--
  Row format:
  | invoice-tax-calculation | 8/8 ✅ | 2026-08-14 · Priya | Charge tax inclusive of the displayed price for EU accounts |
  | renumber                | 3/11   | 2026-09-01 · Chris | Re-sequence invoice line items after a mid-list delete      |
  | employee-directory      | spec only | 2026-07-02 · Sam | Searchable directory with org-chart rollup                 |

  Status values: "N/M" (some slices done), "N/N ✅" (all done), "spec only" (no plan.md
  yet), "no slices" (a plan.md with no checkboxes).
-->
